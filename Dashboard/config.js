// 자동 생성 (scripts/export_dashboard.py) — 라이브 전용
// 속성 추론(/infer)·네트워크 생성(/combo) 모두 라이브. 정적 캐시(trendAttrs 등) 없음.
window.DASHBOARD_DATA = {
 "_meta": {"serving_exp": "v2_sweepA", "note": "live-only (no static cache)"}
};
