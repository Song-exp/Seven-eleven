# -*- coding: utf-8 -*-
"""npd_success_formula_report.md 재조립 — §1~§3 보존 + §0/§4카테고리/§5대시보드/§6수칙정정/§7전체목록 복원."""
import pandas as pd

P = 'docs/npd_success_formula_report.md'
t = open(P, encoding='utf-8').read()

# 1) 모델 라인 정정
t = t.replace('HIN-GNN (`v2_sweepA` & `exp47`)', 'HIN-GNN (`v2_sweepA`, leak-free, test PR-AUC 0.608)')

SEC0 = '''## 0. 이 문서의 위치 — "글로벌 룰"이 아니라 "2025 사전탐지(prior)"

> **관점 정리.** 아래 killer/mine/매개 분류와 §7 키워드 목록은 **2025 데이터에서 미리 탐지한 경향(prior)**이지, *"이 키워드는 항상 성공/회피"라는 글로벌 룰이 아니다.*

| | ❌ 위험한 해석 | ✅ 올바른 해석 |
|---|---|---|
| killer | "넣으면 항상 성공" | "2025엔 성공 라인에 자주 등장한 경향" |
| **mine** | **"무조건 회피(블랙리스트)"** | **"2025엔 약했던 카테고리 경향 (주의 prior)"** |
| 매개 | "어디든 +" | "2025엔 여러 조합에서 보편적으로 +였던 경향" |

**왜 글로벌 "무조건 회피"가 위험한가**: ① 2025 클러스터 학습 결과라 시한부 ② 같은 키워드도 조합 맥락에 따라 보완/잠식이 갈림 ③ mine을 금지룰로 박으면 그 카테고리를 안 만들어 성공 데이터가 영영 안 쌓임(자기실현 편향).

**역할 분담**: 이 문서(CSV)=**prior(사전확률·힌트)** / 대시보드=**실제 판정**(한 키워드를 누르면 그 서브네트워크 맥락에서 라이브로 보완/잠식 결정). mine도 맥락 증거가 뒤집으면 도전 가능.

---

'''

# 2) §0 삽입
i1 = t.index('## 🗺️ 1.')
t = t[:i1] + SEC0 + t[i1:]

# 3) §2-B mine 고분산 보정 노트
MINE_NOTE = '''> ⚠ **mine은 "모달 부진 prior"이지 "100% 실패"가 아니다 (제품 확인)**: 위 mine도 실행에 따라 성공한다 — **와인**은 하정우 마키키(1.8억)·파이퍼하이직(7,796만) 등 *프리미엄은 대박*, **제육**은 맛장우 반반제육(3,348만)처럼 *김밥/도시락 포맷이면 성공*, **건강**은 *단백질/프로틴 트렌드가 성공*. → mine은 회피 우선순위(prior)일 뿐, 글로벌 금지룰이 아니다(§0).'''
marker = '\n\n---\n\n## ⚾ 3.'
t = t.replace(marker, '\n\n' + MINE_NOTE + marker, 1)

# 4) recipe(§4)를 잘라내고 정정본으로 교체 + 카테고리/대시보드/전체목록 추가
i4 = t.index('## 🎯 4.')
body = t[:i4]

SEC4 = '''## 4. 유형별 키워드 카테고리 (데이터 클러스터 + 제품 질적 검토)

> **방법**: 제품 동시출현 클러스터링으로 데이터 그룹을 잡고, **각 그룹의 실제 제품을 읽어** 카테고리를 명명·보정했다(주관적 테마가 아니라 *데이터 군집 + 제품 근거*).
> ⚠ 클러스터링만으론 고빈도 killer가 한 덩어리로 뭉친다(성공작이 한정+촉촉+빵+콜라보를 한꺼번에 달아 함께 등장). 분리 안 되는 부분은 *제품을 보고 KIND를 구분*했다. 등급: ★★★(구조적)/★★(원리)/★(전략·니치)/✗(시한부).

### 🟢 killer

| 카테고리 | 키워드 | 제품에서 본 실체 | 일반화 |
|---|---|---|---|
| **기획 레버** | 한정·시즌·콜라보·띠부씰·키링 | 빼빼로·단팥빵·김밥·도시락·버거 — **카테고리 무관 전 제품**에 얹힘(의사결정 속성) | **★★★** |
| 질감(texture) | 부드러움·촉촉·쫀득함·바삭·말랑함 | 참치마요삼각·함박도시락·새우튀김김밥·메론킥 — **짠맛/단맛 공통**(※베이커리 한정 아님, 질감 일반) | ★★ |
| 베이커리·제과 제법 | 페스츄리·소보로·카스테라·모나카·무스·카이막 | 블루베리패스츄리·소보로삼각·미각카스테라롤·카이막케익 — **디저트 전용**(클러스터 분리됨) | ★★ |
| 맛·풍미 | 매콤·새콤·고소·진함·땡초 | 고추장불고기·반반제육·대게김밥 — **주로 식사류** | ★★ |
| 지역·원산지 | 고창·하와이·대게·미역·쑥·옥수수 | 고창꿀고구마·울진대게·하와이안쉬림프·제주쑥 — **원물 스토리텔링**(클러스터 분리됨) | ★ |
| 에너지·건강음료 | 비타민·타우린·박카스·주스·토닉 | 박카스·얼박사·모닝케어·비타민워터 — **기능성 음료**(클러스터 분리됨) | ★ 니치 |
| 특정 브랜드/IP | 가나·빼빼로·빈츠·메타몽·공룡 | 2025 특정 성공 라인 | **✗** 시한부 |

> 보정: 클러스터는 위 1~4를 한 번들로 묶지만, **제품을 보면 KIND가 다르고 MD 액션도 다르다** — 기획레버=기획 의사결정 / 질감·맛=제품 속성 / 제과제법·지역=카테고리·소싱.

### 🔴 mine — ⚠ 고분산: "모달 실패 + 일부 성공"

| 카테고리 | 키워드 | 제품에서 본 실체 |
|---|---|---|
| 주류·양조 | 와인·하이볼·사케·숙성·화이트 | 다수 실패하나 **프리미엄은 대박**(하정우 와인 1.8억·파이퍼하이직 7,796만) |
| 무거운식사·국물 | 갈비·제육·찌개·마라·사골·삼겹살 | 찌개·사골·탕은 명확 실패, **제육·갈비는 김밥/도시락 포맷이면 성공**(반반제육 3,348만) |
| 건강·다이어트 | 다이어트·운동·칼로리·숙취·디카페인 | **단백질/프로틴은 성공**, 숙취해소·디카페인·제로칼로리는 실패 |
| 향신료·자극 | 시나몬·향신료·산미·마라·쓴맛 | 이질적 향·자극 — 대체로 약함 |

### 🟣 매개

| 카테고리 | 키워드 | 대표 제품 |
|---|---|---|
| 단맛·디저트 축 | 말차·케이크·꿀·망고·고구마·블루베리 | 빼빼로말차·곰발바닥꿀빵·고구마팡 |
| 짭짤·식사 축 | 샌드·라면·국물·샐러드·튀김·반찬 | 햄에그샌드·도쿠시마라면·치킨난반정식 |
| **기능(건강 트렌드)** | 저당·닭가슴살·당충전·곤약 | **저당고추장김밥(2,193만)·머스타드닭가슴살김밥(1,814만) — 성공** |
| 콜라보·브랜드 | 허쉬·쿠앤크·산리오·캐릭터 | 허쉬초코콘·오레오샌드·산리오빼빼로 |

### 📌 종합 — 일반화 등급

| 등급 | 무엇 |
|---|---|
| **확실 일반화** | killer 기획레버(한정·콜라보·굿즈) · 매개 기능(저당·고단백) · mine 카테고리의 *모달* 부진 |
| **카테고리·소싱 전략** | killer 베이커리 제과제법·지역원산지·에너지음료 |
| **검증으로 바로잡은 것** | "베이커리 식감"→질감 일반(짠맛에도) · "와인=실패"→**고분산(프리미엄 성공)** · "건강=실패"→**프로틴 성공** |
| **시한부** | 특정 브랜드 IP · 트렌드 맛 |

---

## 5. 대시보드 연결 — prior는 색이 아니라 맥락 판정으로

대시보드는 이 문서의 글로벌 태그를 **노드 색으로 칠하지 않는다**(키워드=초록·제품=빨강·IP=주황 타입색 유지). 대신 **한 키워드를 클릭하면 그 서브네트워크 맥락에서의 값**(보완/잠식 시너지 Δ, 경로 구간 Δ)을 라이브로 보여준다. killer/mine/매개는 *그 조합 안에서 그때그때 결정*되며, 조건부 killer(공룡=IP 동반)·mine 맥락 예외(특정 조합의 와인)를 자동 처리한다.

> **요약**: §7 목록 = *사전탐지 prior*. "이 조합에서 이 키워드가 성공에 기여하나"는 대시보드가 맥락에서 답한다.

---

## 6. MD 상품 기획 황금 레시피 (정정본)

```
[강한 흥행 killer 최소 1개로 컨셉 천장 확보]
   ➡️ [지뢰(mine) 카테고리는 "주의 prior"로 맥락 재확인]
   ➡️ [나머지 자리는 killer·매개로 — 신호 없는 일반어 "나열"만 피하기]
```

1. **킬러는 "억지로 추가"가 아니라 "강한 컨셉이면 자연히 동반" (상관 ≠ 인과)**
   - 인과(개입): 첫 killer가 천장(~0.65), 2번째+는 ~0(포화) → 억지로 더 넣어도 이득 없음.
   - 상관(실제): 강한 베이커리 라인은 killer가 자연히 2~4개 딸려옴 → 성공작 killer 평균이 높게 관측.
   - 함의: killer 1개로 천장 잡되, 따라오는 killer는 그대로(희석 방어 가드레일). 무관한 killer 억지 삽입만 무의미.
2. **위험은 "일반어 개수"가 아니라 "killer 부족 + 일반어만 나열"**
   - ⚠ "일반어 N개 이하"는 **실데이터로 뒷받침 안 됨** — KBO 성공·실패 일반어가 4.5 ≈ 4.6 동일, 거인단팥빵은 일반 6개로 성공(2,807만).
   - 꼬막·닭강정 실패는 일반어가 많아서가 아니라 **killer가 1개뿐**이라서. killer가 받쳐주면 서술적 일반어는 무방.
   - 함의: 일반어 개수를 세지 말고 **killer를 먼저 확보**하라.
3. **지뢰(mine)는 "금지룰"이 아니라 "주의 prior" — 맥락 재확인**
   - 와인·제육·다이어트 등은 2025에 모달 부진이나, **프리미엄·포맷·트렌드 실행이면 성공**(§4 mine 고분산). 글로벌 블랙리스트로 박지 말고 대시보드 맥락 시너지로 재확인.
   - 금지룰로 카테고리를 영영 안 만들면 그 카테고리 성공 데이터 자체가 안 쌓인다(자기실현 편향).

> **일반화되는 것**: "강한 killer ≥1 확보 + mine 카테고리 *주의*"(원리). **일반화 안 되는 것**: "일반어 N개 이하" 같은 하드 숫자, 어떤 키워드가 killer/mine인지(2025 시한부, 2026 재학습).

---

'''

body = body + SEC4

# 5) §7 전체 키워드 목록 (CSV 생성)
df = pd.read_csv('data/processed/hin/keyword_final.csv')
def fnum(x):
    try:
        return '%.2f' % float(x)
    except Exception:
        return '—'
L7 = ['## 7. 전체 키워드 사전탐지 목록 (2025 prior)', '',
      '> 2025 그래프에서 탐지된 killer/mine/매개 **전체**. 값은 *2025 경향*이며 글로벌 룰이 아니다(§0).',
      '> 컬럼: `빈도`·`purity`(base 0.238)·`단독성공률`·`Δprob`(인과)·`판정`', '']
specs = [('killer', '🟢 7-1. killer (성공 경향)', 'delta', False),
         ('mine', '🔴 7-2. mine (실패 경향 / 주의 prior)', 'delta', True),
         ('매개', '🟣 7-3. 매개 (보편 강화 경향)', 'delta_pos', False)]
for tag, hdr, sk, asc in specs:
    sub = df[df.tag == tag].sort_values(sk, ascending=asc, na_position='last')
    L7 += ['### %s — %d개' % (hdr, len(sub)), '',
           '| 키워드 | 빈도 | purity | 단독성공률 | Δprob | 판정 |', '|---|---|---|---|---|---|']
    for _, r in sub.iterrows():
        L7.append('| %s | %d | %.2f | %s | %s | %s |' % (
            r['keyword'], int(r['빈도']), float(r['purity']),
            fnum(r['단독성공률']) if pd.notna(r['단독성공률']) else '—',
            fnum(r['delta']) if pd.notna(r['delta']) else '—',
            str(r['판정']) if pd.notna(r['판정']) else '—'))
    L7.append('')
L7.append('> ※ killer 중 인과검증(Δprob)에서 "가짜(상관만)"로 판정된 35개는 neutral 강등되어 위 목록에 없다(관찰+인과 통과분만). 최종 판정은 대시보드 맥락이 한다(§0·§5).')

final = body + '\n'.join(L7) + '\n'
open(P, 'w', encoding='utf-8').write(final)
print('재조립 완료. 줄수:', final.count('\n'))
print('killer %d / mine %d / 매개 %d' % ((df.tag == 'killer').sum(), (df.tag == 'mine').sum(), (df.tag == '매개').sum()))
