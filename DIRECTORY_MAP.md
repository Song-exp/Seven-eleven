# 📂 7-Eleven NPD AI Framework Directory Map

본 문서는 프로젝트의 새로운 디렉토리 구조와 각 폴더의 역할을 정의합니다. 모든 팀원은 파일 생성 및 관리 시 아래 규칙을 준수해야 합니다.

---

## 🏗️ Core Framework (핵심 로직)

### `src/` - 소스 코드
프로젝트의 핵심 비즈니스 로직 및 모델 구현체가 포함됩니다.
*   `data_builder/`: 데이터 추출, 노드/엣지 생성 및 HIN 그래프 구축 로직
*   `models/`: GNN(HGT, KGAT 등) 모델 아키텍처 정의
*   `train/`: 모델 학습 루프 및 최적화 스크립트
*   `eval/`: 모델 성능 평가 및 지표 분석

### `preprocessing/` - 데이터 전처리
원천 데이터를 학습 가능한 형태로 가공하는 단계별 파이프라인입니다.
*   `pipeline/`: 실제 실행되는 단계별 전처리 스크립트 (`pipeline0` ~ `pipeline5`)
*   `research/`: 전처리 로직 설계를 위한 실험용 노트북 및 기획 문서

---

## 📊 Analysis & EDA (분석 및 시각화)

### `eda/` - 탐색적 데이터 분석
데이터를 이해하고 인사이트를 도출하는 모든 분석 활동의 결과물입니다.
*   `notebooks/`: 모든 분석용 `.ipynb` 파일 (개인 작업물은 `archive/`에 보관)
*   `visualizations/`: 분석 결과로 생성된 모든 이미지(`png`) 및 인터랙티브 그래프(`html`)
*   `data_outputs/`: 분석 과정에서 생성된 통계 데이터(`csv`, `xlsx`) 및 중간 결과물

### `scripts/` - 실행 도구 및 유틸리티
코드 베이스 외부에서 실행되는 보조 도구들입니다.
*   `analysis/`: 파레토 분석, 카테고리별 매출 분석 등 특정 목적의 분석 스크립트
*   `utils/`: 패치(`patch_*.py`), 상태 점검(`check_*.py`), 데이터 검증(`verify_*.py`) 도구

---

## 📦 Data & Documents (데이터 및 문서)

### `data/` - 데이터 저장소
*   `raw/`: 수집된 원본 데이터 (인스타그램 크롤링, POS 원천 등)
*   `processed/`: 전처리가 완료되어 즉시 학습에 사용 가능한 `.parquet` 파일

### `docs/` - 문서화
*   `specifications/`: 아키텍처 설계, 에이전트 규칙 등 시스템 명세서
*   `reports/`: 데이터 수집 결과, 모델 성능 보고서 등 주기적 리포트

---

## 🗄️ Others (기타)

### `archive/` - 보관소
현재 프로젝트의 메인 파이프라인에서는 사용되지 않지만, 기록 보존이 필요한 파일들입니다.
*   `dashboard_midterm/`: 중간 발표용 대시보드 서비스 코드 및 관련 데이터
*   과거 버전의 코드 및 일회성 작업물

### `config/` - 설정
*   `model_config.yaml`: 하이퍼파라미터 및 경로 설정 파일

### `logs/` - 실행 기록
*   시스템 구동 및 에러 로그 파일
